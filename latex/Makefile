report:
	latexmk -pvc report.tex

proposal:
	latexmk -pvc proposal.tex

presentation:
	latexmk -pvc presentation.tex

clean:
	latexmk -C
	-rm *.lol
	-rm *.bbl
	-rm *.run.xml
	-rm -r _minted*
	-rm *.nav
	-rm *.pyg
	-rm *.snm
	-rm *.vrb
	-rm *.fls
