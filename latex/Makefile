report:
	latexmk -pvc report.tex

proposal:
	latexmk -pvc proposal.tex

presentation01:
	latexmk -pvc presentation01.tex

clean:
	latexmk -C
	-rm *.lol
	-rm *.bbl
	-rm *.run.xml
	-rm -r _minted*
	-rm *.nav
	-rm *.pyg
	-rm *.snm
	-rm *.vrb
	-rm *.fls
